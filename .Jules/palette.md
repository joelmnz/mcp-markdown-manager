## 2025-01-23 - Code Block Interaction
**Learning:** `ReactMarkdown` allows wrapping `pre` elements via the `components` prop. This is the ideal place to inject "Copy Code" buttons or other block-level actions without parsing the content manually.
**Action:** Use this pattern for any future code block enhancements (e.g., language labels, execution buttons).
